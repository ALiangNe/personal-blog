{
  "version": 2,
  "buildCommand": "mkdir -p public && cp -r dao/* public/",
  "outputDirectory": "public",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
} 